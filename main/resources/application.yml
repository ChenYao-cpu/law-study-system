server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/minzu_law?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root          # 您的 MySQL 用户名
    password: 123456        # 您的 MySQL 密码
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto